services: 
  web: 
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - FLASK_DEBUG=${FLASK_DEBUG:-0}
    volumes:
      - ./instance:/app/instance
    restart: unless-stopped